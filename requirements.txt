# --- Core service ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
tenacity==9.0.0
structlog==24.4.0

# --- PDF handling / rendering ---
pymupdf==1.25.1          # fast PDF -> image rendering, native text extraction
pillow==11.1.0

# --- Parser backends (optional at runtime; router degrades gracefully) ---
docling==2.15.1          # IBM open-source layout-aware parser (self-hosted)
llama-cloud-services==0.6.94  # LlamaParse client (API). 0.6.0 left llama-cloud
                              # unpinned, so pip resolved llama-cloud 1.x which
                              # dropped a symbol 0.6.0 imported -> ImportError.
llama-cloud==0.1.46           # pin the transitive dep to the verified pair

# --- LLM provider SDKs (optional; loaded lazily by provider) ---
openai==2.40.0           # OpenAI + Azure OpenAI (GPT-5.x: max_completion_tokens, no temperature)
anthropic==0.105.2       # Claude (4.x models: no temperature/top_p)

# --- Q&A document output ---
markdown-it-py==3.0.0

# --- Dev / test ---
pytest==8.3.4
pytest-asyncio==0.25.2
respx==0.22.0
